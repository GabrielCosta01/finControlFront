import { ReactNode } from 'react';
import ClientLayout from '@/components/ClientLayout';

export default function ExtraIncomeLayout({ children }: { children: ReactNode }) {
  return <ClientLayout>{children}</ClientLayout>;
} 